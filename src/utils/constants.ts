export const VEHICLE_TYPES = ["car", "bike", "van", "SUV"] as const;
export const VEHICLE_STATUSES = ["available", "booked"] as const;
export const BOOKING_STATUSES = ["active", "cancelled", "returned"] as const;
export const USER_ROLES = ["admin", "customer"] as const;
